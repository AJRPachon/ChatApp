<?xml version="1.0" encoding="UTF-8"?>
<lint>
    <!--
        App Links (autoVerify="true") requires a real https:// domain with a hosted
        assetlinks.json. This is intentionally deferred until a production domain
        is configured. Suppressed here to keep lint green on the CI.
    -->
    <issue id="AppLinkUrlError" severity="ignore"/>
</lint>
