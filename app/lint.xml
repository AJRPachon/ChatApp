<?xml version="1.0" encoding="UTF-8"?>
<lint>
    <!--
        App Links (autoVerify="true") requires a real https:// domain with a hosted
        assetlinks.json. Deferred until a production domain is configured.
    -->
    <issue id="AppLinkUrlError" severity="ignore"/>

    <!--
        ByteOrderMark in local.properties is a CI/build-config artifact, not
        source code. local.properties is gitignored and regenerated by CI.
    -->
    <issue id="ByteOrderMark" severity="ignore"/>
</lint>
